{-# LANGUAGE TypeApplications #-}
{-# LANGUAGE ScopedTypeVariables #-}
{-# LANGUAGE FlexibleContexts #-}

-- | CIFilter are filter objects for Core Image that encapsulate the filter with its attributes
--
-- The CIFilter class produces a CIImage object as output. Typically, a filter takes one or more images as input. Some filters, however, generate an image based on other types of input parameters. The parameters of a CIFilter object are set and retrieved through the use of key-value pairs. You use the CIFilter object in conjunction with the CIImage, CIContext, CIVector, CIImageAccumulator, and CIColor objects to take advantage of the built-in Core Image filters when processing images. CIFilter objects are also used along with CIKernel, CISampler, and CIFilterShape objects to create custom filters.
--
-- Generated bindings for @CIFilter@.
module ObjC.Quartz.CIFilter
  ( CIFilter
  , IsCIFilter(..)
  , viewForUIConfiguration_excludedKeys
  , viewForUIConfiguration_excludedKeysSelector


  ) where

import Foreign.Ptr (Ptr, nullPtr, castPtr)
import Foreign.LibFFI
import Foreign.C.Types
import Data.Int (Int8, Int16)
import Data.Word (Word16)
import Data.Coerce (coerce)

import ObjC.Runtime.Types
import ObjC.Runtime.MsgSend (sendMsg, sendClassMsg)
import ObjC.Runtime.Selector (mkSelector)
import ObjC.Runtime.Class (getRequiredClass)

import ObjC.Quartz.Internal.Classes
import ObjC.CoreImage.Internal.Classes
import ObjC.Foundation.Internal.Classes

-- | viewForUIConfiguration:excludedKeys:
--
-- The viewForUIConfiguration returns a IKFilterView for the filter. The UI is either created by the filter or automatically generated by the CoreImageKit framework.
--
-- The viewForUIConfiguration first requests that the filter provides a UI by calling provideViewForUIConfiguration. If this method is not implemented or the filter cannot provide a UI for the given configuration, the CoreImageKit framework will generate a UI.		The UIConfiguration dictionary is an NSDictionary with the following defined keys:
--
-- @UIConfiguration@ — Dictionary with the IKUISizeFlavor and the kCIUIParameterSet information - see below.
--
-- @excludedKeys@ — An array of keys that should not be included in the view (for example inputImage will often be excluded). This can be nil, if no keys should be excluded
--
-- IKUISizeFlavor:
--
-- mini:		IKUISizeMini
--
-- small:		IKUISizeSmall
--
-- regular:	IKUISizeRegular
--
-- These sizes follow the size conventions available in Tiger IB
--
-- kCIUIParameterSet:
--
-- basic:			kCIUISetBasic
--
-- intermediate:		kCIUISetIntermediate
--
-- advanced:		kCIUISetAdvanced
--
-- development:	kCIUISetDevelopment
--
-- When a client app requests a UI for a parameter set, all keys for that set and below will be included - example: advanced consists of all parameters in the basic, intermediate and advanced set.The development set is targeted towards the development of filters and client apps. Parameters in this set are either experimental or for debugging purposes and should not be shown in a shipping product.IKUImaxSize:(NSSize)maxSize;	Maximum allowed dimension of the returned view. If the size requested is too small, the filter is expected to return a view as small as possible. It is up to the client to verify that the returned view fits into his context.IKUIFlavorAllowFallback:Boolean	If a requested flavor set-size combination is not supported, the filter can return a view for a different set-size combination. If this is set to no (which is the default), NULL should be returned by the filter
--
-- ObjC selector: @- viewForUIConfiguration:excludedKeys:@
viewForUIConfiguration_excludedKeys :: (IsCIFilter ciFilter, IsNSDictionary inUIConfiguration, IsNSArray inKeys) => ciFilter -> inUIConfiguration -> inKeys -> IO (Id IKFilterUIView)
viewForUIConfiguration_excludedKeys ciFilter  inUIConfiguration inKeys =
withObjCPtr inUIConfiguration $ \raw_inUIConfiguration ->
  withObjCPtr inKeys $ \raw_inKeys ->
      sendMsg ciFilter (mkSelector "viewForUIConfiguration:excludedKeys:") (retPtr retVoid) [argPtr (castPtr raw_inUIConfiguration :: Ptr ()), argPtr (castPtr raw_inKeys :: Ptr ())] >>= retainedObject . castPtr

-- ---------------------------------------------------------------------------
-- Selectors
-- ---------------------------------------------------------------------------

-- | @Selector@ for @viewForUIConfiguration:excludedKeys:@
viewForUIConfiguration_excludedKeysSelector :: Selector
viewForUIConfiguration_excludedKeysSelector = mkSelector "viewForUIConfiguration:excludedKeys:"

